# java_sparse_matrix_csr_hadoop
sparse matrix-vector multiplication, namely SpMV, in a parallel computing framework. SpMV is the computation of the following form: ~y = A~x, where A is a sparse matrix, ~y and ~x are dense vectors. Page
